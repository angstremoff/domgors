# 🔑 Как получить правильный ключ и настроить GitHub

Google Play требует ключ с отпечатком `BF:00...`. Он хранится на серверах Expo. Самый простой способ его достать — через сайт.

## 1. Скачайте Keystore с сайта Expo

1. Зайдите на: **[Expo Credentials Dashboard](https://expo.dev/accounts/angstremoff/projects/DomGoMobile/credentials/android)**
2. Найдите в списке **"Build Credentials"** профиль с **SHA1 Fingerprint**, начинающимся на `BF:00...` (скорее всего он самый старый или помечен как Production).
3. Нажмите на него.
4. Скачайте файл **Keystore** (обычно называется `application-0-....jks` или подобное).
5. **Сохраните его на Рабочий стол** и переименуйте в `expo-keystore.jks`.
6. Там же на сайте скопируйте:
   - **Keystore Password**
   - **Key Alias**
   - **Key Password**

## 2. Подготовьте ключ для GitHub

Когда файл `expo-keystore.jks` будет на Рабочем столе, выполните в терминале эту команду, чтобы получить Base64 код:

```bash
base64 -i ~/Desktop/expo-keystore.jks | pbcopy
```
*(Код сразу скопируется в буфер обмена)*

## 3. Добавьте секреты в GitHub

Зайдите в **[Settings -> Secrets and variables -> Actions](https://github.com/angstremoff/domgomobile/settings/secrets/actions)** и создайте 4 секрета:

1. **`KEYSTORE_BASE64`**: (Вставьте то, что скопировали командой выше)
2. **`KEYSTORE_PASSWORD`**: (Пароль с сайта Expo)
3. **`KEY_ALIAS`**: (Alias с сайта Expo)
4. **`KEY_PASSWORD`**: (Пароль ключа с сайта Expo)

## 4. Запустите сборку

Зайдите в **[Actions](https://github.com/angstremoff/domgomobile/actions)** -> **Build Android AAB** -> **Run workflow**.

🚀 Это соберет правильный AAB, который примет Google Play.
